%section
  %h2 each_with_object
  %h5 A simpler version of inject for building an object
  %pre
    %code{"data-trim" => ""}
      :preserve
        users = User.where(admin: true)
        admin_name_count = users.each_with_object(Hash.new(0)) do |user, hsh|
          hsh[user.name] += 1
        end
        admin_name_count # => {"mike": 1, "john": 2, "dave": 1}
